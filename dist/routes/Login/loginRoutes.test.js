describe('', function () {
    test('it should respond with 200 success', () => {
        const response = 200;
        expect(response).toBe(200);
    });
});
export {};
//# sourceMappingURL=loginRoutes.test.js.map